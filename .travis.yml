language: cpp

include:
    - os: linux
      addons:
          apt:
              sources:
                  - ubuntu-toolchain-r-test
              packages:
                  - g++-7

before_install:
    - export CXX=g++-7

script:
    - make
